name: Push to ECR

on:
  workflow_run:
    workflows: ["Build"]
    types:
      - completed

jobs:
  push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source code
        uses: actions/checkout@v4

      - name: Download image URI artifact
        uses: actions/download-artifact@v4
        with:
          name: image-uri

      - name: Read IMAGE_URI
        run: echo "IMAGE_URI=$(cat image_uri.txt)" >> $GITHUB_ENV

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ vars.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Pull and push image to Amazon ECR
        run: |
          docker pull $IMAGE_URI
          docker tag $IMAGE_URI ${{ secrets.ECR_REGISTRY }}/${{ vars.REPO_NAME }}:latest
          docker push ${{ secrets.ECR_REGISTRY }}/${{ vars.REPO_NAME }}:latest
